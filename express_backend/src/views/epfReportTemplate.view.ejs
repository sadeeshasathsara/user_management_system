<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>EPF Report - <%= employee.name %>
  </title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      line-height: 1.4;
      color: #000;
      padding: 0;
      background: white;
    }

    .report-header {
      margin-bottom: 20px;
    }

    .header-top {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 2px solid #333;
    }

    .logo-container img {
      width: 100px;
      height: auto;
    }

    .header-title {
      flex: 1;
      text-align: center;
      font-size: 12pt;
      font-weight: bold;
      color: #1a1a1a;
      padding: 0 15px;
      line-height: 1.3;
    }

    .year-badge {
      text-align: right;
      min-width: 80px;
    }

    .year-label {
      font-size: 8pt;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 2px;
    }

    .year {
      font-size: 13pt;
      color: #333;
      font-weight: bold;
      line-height: 1;
    }

    .section {
      margin-bottom: 18px;
      page-break-inside: avoid;
    }

    .section-title {
      font-size: 11pt;
      font-weight: bold;
      color: #1a1a1a;
      margin-bottom: 8px;
      padding: 6px 0;
      border-bottom: 2px solid #666;
    }

    .info-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 6px;
    }

    .info-table td {
      padding: 14px 12px;
      border: 1px solid #d0d0d0;
      font-size: 11pt;
    }

    .info-table td:first-child {
      width: 180px;
      font-weight: bold;
      background-color: #f5f5f5;
      color: #1a1a1a;
    }

    .data-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
      font-size: 11pt;
    }

    .data-table thead th {
      background-color: #333;
      color: white;
      padding: 14px 12px;
      text-align: left;
      font-weight: bold;
      border: 1px solid #333;
      font-size: 11pt;
    }

    .data-table thead th:last-child {
      text-align: right;
    }

    .data-table tbody td {
      padding: 11px 12px;
      border: 1px solid #ddd;
    }

    .data-table tbody td:last-child {
      text-align: right;
      font-weight: 600;
    }

    .range-row {
      background-color: #fafafa;
    }

    .range-row td:first-child {
      font-weight: bold;
      color: #1a202c;
    }

    .details-container {
      padding: 0;
      background-color: #f9f9f9;
    }

    .details-table {
      width: 96%;
      margin: 15px auto;
      border-collapse: collapse;
      font-size: 10pt;
    }

    .details-table thead th {
      background-color: #e8e8e8;
      padding: 9px 12px;
      text-align: left;
      font-weight: bold;
      border: 1px solid #ccc;
      color: #333;
    }

    .details-table thead th:last-child {
      text-align: right;
    }

    .details-table tbody td {
      padding: 8px 12px;
      border: 1px solid #e0e0e0;
      background-color: white;
    }

    .details-table tbody td:last-child {
      text-align: right;
      font-weight: 600;
    }

    .subtotal-row {
      background-color: #e8f4f8 !important;
      font-weight: bold;
    }

    .subtotal-row td {
      padding: 14px 12px !important;
      border: 1px solid #999 !important;
      font-size: 11pt;
    }

    .total-row {
      background-color: #333 !important;
      color: white !important;
      font-weight: bold;
      font-size: 13pt;
    }

    .total-row td {
      padding: 16px 12px !important;
      border: 1px solid #333 !important;
    }

    .spacer {
      height: 15px;
    }

    .footer {
      margin-top: 50px;
      padding-top: 20px;
      border-top: 2px solid #333;
      text-align: center;
      font-size: 9pt;
      color: #666;
    }

    .footer p {
      margin: 5px 0;
    }

    /* PDF-specific optimizations */
    @page {
      margin: 20mm;
    }

    @media print {
      body {
        padding: 0;
      }

      .section {
        page-break-inside: avoid;
      }

      .data-table {
        page-break-inside: auto;
      }

      .data-table tr {
        page-break-inside: avoid;
        page-break-after: auto;
      }
    }
  </style>
</head>

<body>
  <div class="report-header">
    <div class="header-top">
      <div class="logo-container">
        <img src="<%= logoDataUri %>" alt="Company Logo">
      </div>
      <div class="header-title">
        Details of the State Pharmaceuticals Corporation Welfare Medical Allowance
      </div>
      <div class="year-badge">
        <div class="year-label">Year</div>
        <div class="year">
          <%= year %>
        </div>
      </div>
    </div>
  </div>

  <div class="section">
    <div class="section-title">Employee Information</div>
    <table class="info-table">
      <tr>
        <td>Name</td>
        <td>
          <%= employee.name %>
        </td>
      </tr>
      <tr>
        <td>EPF Number</td>
        <td>
          <%= employee.epfNumber %>
        </td>
      </tr>
      <tr>
        <td>Department</td>
        <td>
          <%= employee.department?.name || '-' %>
        </td>
      </tr>
      <tr>
        <td>Email</td>
        <td>
          <%= employee.email || '-' %>
        </td>
      </tr>
      <tr>
        <td>Location</td>
        <td>
          <%= employee.mainLocation || '-' %>
        </td>
      </tr>
    </table>
  </div>

  <div class="section">
    <div class="section-title">Summary</div>
    <table class="info-table">
      <tr>
        <td>Total Medical Expenses</td>
        <td>
          <%= totalExpenses.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
        </td>
      </tr>
      <tr>
        <td>Local Medical Expenses</td>
        <td>
          <%= rangeSummaries.reduce((sum,r)=> sum + r.totalAmount, 0).toLocaleString('en-US', { minimumFractionDigits:
            2, maximumFractionDigits: 2 }) %>
        </td>
      </tr>
      <tr>
        <td>English Medical Expenses</td>
        <td>
          <%= regularTotal.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %> (<%=
              regularCount %> times)
        </td>
      </tr>
    </table>

    <div style="margin-top: 18px;">
      <div class="section-title">Medical Usage Overview</div>
      <table class="info-table">
        <tr>
          <td>Used</td>
          <td>
            <%= medicalUsed.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
          </td>
        </tr>
        <tr>
          <td>Remaining</td>
          <td>
            <%= medicalRemaining.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
          </td>
        </tr>
        <tr>
          <td>Limit</td>
          <td>
            <%= medicalLimit.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
          </td>
        </tr>
      </table>
    </div>
  </div>

  <div class="section">
    <div class="section-title">EPF Range Usage Summary</div>
    <table class="data-table">
      <thead>
        <tr>
          <th>Medicine Type</th>
          <th style="width: 120px;">Times Used</th>
          <th style="width: 180px;">Total Amount (Rs.)</th>
        </tr>
      </thead>
      <tbody>
        <% rangeSummaries.forEach(range=> { %>
          <tr class="range-row">
            <td>
              <%= range.name %>
            </td>
            <td>
              <%= range.count %>
            </td>
            <td>
              <%= range.totalAmount.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
            </td>
          </tr>
          <% if (range.expenseDetails.length> 0) { %>
            <tr>
              <td colspan="3" class="details-container">
                <table class="details-table">
                  <thead>
                    <tr>
                      <th>Date</th>
                      <th style="width: 180px;">Amount (Rs.)</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% range.expenseDetails.forEach(exp=> { %>
                      <tr>
                        <td>
                          <%= exp.date %>
                        </td>
                        <td>
                          <%= exp.amount.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
                            %>
                        </td>
                      </tr>
                      <% }) %>
                  </tbody>
                </table>
              </td>
            </tr>
            <% } %>
              <% }) %>

                <tr class="spacer">
                  <td colspan="3" style="border: none; background: white;"></td>
                </tr>

                <tr class="subtotal-row">
                  <td>Regular EPF Expenses</td>
                  <td>—</td>
                  <td>
                    <%= regularTotal.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
                  </td>
                </tr>

                <tr>
                  <td colspan="3" class="details-container">
                    <table class="details-table">
                      <thead>
                        <tr>
                          <th>Date</th>
                          <th style="width: 180px;">Amount (Rs.)</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% regularExpenses.forEach(exp=> { %>
                          <tr>
                            <td>
                              <%= exp.date %>
                            </td>
                            <td>
                              <%= exp.amount.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits:
                                2 }) %>
                            </td>
                          </tr>
                          <% }) %>
                      </tbody>
                    </table>
                  </td>
                </tr>

                <tr class="spacer">
                  <td colspan="3" style="border: none; background: white;"></td>
                </tr>

                <tr class="total-row">
                  <td colspan="2">TOTAL EPF USED</td>
                  <td>
                    <%= totalUsed.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
                  </td>
                </tr>
      </tbody>
    </table>
  </div>

  <div class="footer">
    <p><strong>Generated on:</strong>
      <%= generatedAt %>
    </p>
    <p>&copy; <%= new Date().getFullYear() %> · UMS · State Pharmaceuticals Corporation</p>
  </div>
</body>

</html>